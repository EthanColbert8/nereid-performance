server:
  bind_addr: "[::1]:50051"

  # This folder must exist in the project root and contain all ML model folders.
  # ml_backends_path: "/depot/cms/users/colberte/SONIC/nereid/nereid-server/ml-backends"
  ml_backends_path: "models"

models:
  # - name: "big_mlp"
  #   device: "cpu"
  #   queue_capacity: 8

  - name: "particlenet_AK4_PT"
    device: "cuda"
    queue_capacity: 8

  - name: "particlenet_AK4"
    device: "cuda"
    queue_capacity: 8

  # Add one entry per model folder under server.ml_backends_path.
  # device can be "cpu" or "cuda".
  # queue_capacity must be greater than 0.